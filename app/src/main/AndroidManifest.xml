<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dgmusic">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SearchAlbumActivity"
            android:exported="true" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlbumActivity" />
        <activity android:name=".AlbumArtistActivity" />
        <activity android:name=".AlbumArtistTrackActivity" />
        <activity android:name=".AlbumTrackActivity" />
        <activity android:name=".ArtistActivity" />
        <activity android:name=".ArtistTrackActivity" />
        <activity android:name=".PlaylistActivity" />
        <activity android:name=".PlaylistTrackActivity" />
    </application>

</manifest>